{%- comment -%}
  ============================================================
  NAV / HEADER — cabeçalho do site
  ============================================================

  O QUE FAZ:
  Monta o cabeçalho da loja. 
  Inclui uma faixa de anúncio animada no topo, logo e menu 
  com links fixos ("O plot twist", "Escolha seu kit", 
  "Universo BRIGZ") para replicar fielmente o design.
{%- endcomment -%}

<header class="header" data-section-id="{{ section.id }}">
  
  <!-- FAIXA ANIMADA (MARQUEE) -->
  <div class="announcement-bar">
    <div class="announcement-bar__track">
      
      <!-- Bloco 1 -->
      <div class="announcement-bar__content">
        <div class="announcement-bar__item">
          <svg class="announcement-star" viewBox="0 0 24 24"><path d="M12 0L13.5 10.5L24 12L13.5 13.5L12 24L10.5 13.5L0 12L10.5 10.5L12 0Z"/></svg>
          ZERO AÇÚCAR
        </div>
        <div class="announcement-bar__item">
          <svg class="announcement-star" viewBox="0 0 24 24"><path d="M12 0L13.5 10.5L24 12L13.5 13.5L12 24L10.5 13.5L0 12L10.5 10.5L12 0Z"/></svg>
          31 KCAL POR COLHER
        </div>
        <div class="announcement-bar__item">
          <svg class="announcement-star" viewBox="0 0 24 24"><path d="M12 0L13.5 10.5L24 12L13.5 13.5L12 24L10.5 13.5L0 12L10.5 10.5L12 0Z"/></svg>
          CACAU 100% PURO
        </div>
        <div class="announcement-bar__item">
          <svg class="announcement-star" viewBox="0 0 24 24"><path d="M12 0L13.5 10.5L24 12L13.5 13.5L12 24L10.5 13.5L0 12L10.5 10.5L12 0Z"/></svg>
          BRIGADEIRO DE OUTRO MUNDO
        </div>
      </div>
      
      <!-- Bloco 2 (Cópia exata para o loop infinito sem quebras) -->
      <div class="announcement-bar__content" aria-hidden="true">
        <div class="announcement-bar__item">
          <svg class="announcement-star" viewBox="0 0 24 24"><path d="M12 0L13.5 10.5L24 12L13.5 13.5L12 24L10.5 13.5L0 12L10.5 10.5L12 0Z"/></svg>
          ZERO AÇÚCAR
        </div>
        <div class="announcement-bar__item">
          <svg class="announcement-star" viewBox="0 0 24 24"><path d="M12 0L13.5 10.5L24 12L13.5 13.5L12 24L10.5 13.5L0 12L10.5 10.5L12 0Z"/></svg>
          31 KCAL POR COLHER
        </div>
        <div class="announcement-bar__item">
          <svg class="announcement-star" viewBox="0 0 24 24"><path d="M12 0L13.5 10.5L24 12L13.5 13.5L12 24L10.5 13.5L0 12L10.5 10.5L12 0Z"/></svg>
          CACAU 100% PURO
        </div>
        <div class="announcement-bar__item">
          <svg class="announcement-star" viewBox="0 0 24 24"><path d="M12 0L13.5 10.5L24 12L13.5 13.5L12 24L10.5 13.5L0 12L10.5 10.5L12 0Z"/></svg>
          BRIGADEIRO DE OUTRO MUNDO
        </div>
      </div>

    </div>
  </div>

  <div class="header__inner">
    <button
      type="button"
      class="header__menu-toggle tap-target"
      aria-label="{{ 'sections.header.menu' | t }}"
      aria-expanded="false"
      aria-controls="MobileNav-{{ section.id }}"
    >
      <span class="header__burger"></span>
    </button>

    <a href="{{ routes.root_url }}" class="header__logo-link">
      {%- if settings.logo != blank -%}
        {%- assign logo_alt = shop.name | escape -%}
        {%- assign logo_height = settings.logo_width | divided_by: settings.logo.aspect_ratio -%}
        {{
          settings.logo
          | image_url: width: 600
          | image_tag:
            class: 'header__logo-image',
            widths: '150, 300, 450, 600',
            width: settings.logo_width,
            height: logo_height,
            alt: logo_alt,
            sizes: '(max-width: 1000px) 40vw, 200px'
        }}
      {%- else -%}
        <span class="header__logo-text">{{ shop.name }}</span>
      {%- endif -%}
    </a>

    <!-- Menu Fixo com os textos da Imagem -->
    <nav class="header__nav" role="navigation" aria-label="{{ 'sections.header.menu' | t }}">
      <ul class="header__menu" role="list">
        <li class="header__menu-item">
          <a href="#" class="header__menu-link">O plot twist</a>
        </li>
        <li class="header__menu-item">
          <a href="#" class="header__menu-link">Escolha seu kit</a>
        </li>
        <li class="header__menu-item">
          <a href="#" class="header__menu-link">Universo BRIGZ</a>
        </li>
      </ul>
    </nav>

    <div class="header__actions">
      <!-- Ocultado via CSS para combinar com o visual da imagem -->
      <details class="header__search">
        <summary class="header__action-button tap-target" aria-label="{{ 'general.search.search' | t }}">
          {% render 'icon', name: 'search' %}
        </summary>
        <div class="header__search-panel">
          <form action="{{ routes.search_url }}" method="get" role="search" class="header__search-form">
            <input
              type="search"
              name="q"
              value="{{ search.terms | escape }}"
              placeholder="{{ 'general.search.placeholder' | t }}"
              aria-label="{{ 'general.search.search' | t }}"
              class="field__input"
            >
            <button type="submit" class="visually-hidden">{{ 'general.search.submit' | t }}</button>
          </form>
        </div>
      </details>

      {%- if shop.customer_accounts_enabled -%}
        <!-- Ocultado via CSS para combinar com o visual da imagem -->
        <a href="{{ routes.account_url }}" class="header__action-button header__account tap-target" aria-label="{{ 'customer.account.title' | t }}">
          {% render 'icon', name: 'account' %}
        </a>
      {%- endif -%}

      <a href="{{ routes.cart_url }}" class="header__action-button header__cart tap-target" aria-label="{{ 'templates.cart.cart' | t }}">
        {% render 'icon', name: 'cart' %}
        <span class="header__cart-count" data-cart-count>{{ cart.item_count }}</span>
      </a>
    </div>
  </div>

  {%- if section.settings.enable_country_selector or section.settings.enable_language_selector -%}
    <div class="header__localization">
      {% render 'localization-form',
        section_id: section.id,
        show_country: section.settings.enable_country_selector,
        show_language: section.settings.enable_language_selector
      %}
    </div>
  {%- endif -%}
</header>

<!-- Menu Mobile Fixo com os textos da Imagem -->
<div class="header__mobile-nav" id="MobileNav-{{ section.id }}" hidden>
  <ul class="header__mobile-menu" role="list">
    <li>
      <a href="#" class="header__mobile-link">O plot twist</a>
    </li>
    <li>
      <a href="#" class="header__mobile-link">Escolha seu kit</a>
    </li>
    <li>
      <a href="#" class="header__mobile-link">Universo BRIGZ</a>
    </li>
  </ul>
</div>

<style>
  /* Importação direta da fonte Outfit Extrabold (800) */
  @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@800&display=swap');

  /* Variáveis baseadas na imagem de referência */
  :root {
    --header-bg: #FDF5E6;
    --header-color-brown: #381A0F;
    --header-color-orange: #F17A04;
    --header-color-cyan: #00FFFF;
  }

  /* --- INÍCIO: FAIXA ANIMADA (MARQUEE) --- */
  .announcement-bar {
    background-color: var(--header-color-brown);
    color: #FFFFFF;
    overflow: hidden;
    white-space: nowrap;
    padding: 10px 0;
    width: 100%;
    position: relative;
    display: flex;
  }

  .announcement-bar__track {
    display: flex;
    width: max-content;
    animation: marquee-scroll 25s linear infinite;
  }

  .announcement-bar__content {
    display: flex;
    align-items: center;
    justify-content: space-around;
  }

  .announcement-bar__item {
    display: flex;
    align-items: center;
    padding: 0 30px; 
    font-family: 'Outfit', sans-serif; 
    font-weight: 800; 
    font-size: 12px; 
    letter-spacing: 0.05em;
    line-height: 1; /* Força o alinhamento central */
  }

  .announcement-star {
    width: 14px;
    height: 14px;
    margin-right: 30px; 
    fill: var(--header-color-cyan);
  }

  @keyframes marquee-scroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); } 
  }
  /* --- FIM: FAIXA ANIMADA --- */


  /* Largura 100% da tela e Borda */
  .header {
    background-color: var(--header-bg);
    border-bottom: 2px solid var(--header-color-brown); /* 2px no desktop */
    width: 100%;
    max-width: 100%;
    margin: 0;
  }
  
  .header__inner {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center; /* Alinha o grid ao centro */
    width: 100%;
    padding: 16px 76px; /* 76px lateral no desktop */
    box-sizing: border-box;
  }
  
  .header__logo-link {
    justify-self: start;
    text-decoration: none;
    display: flex; /* Força que a logo não tenha espaços fantasmas de texto */
    align-items: center;
  }
  
  .header__logo-image { display: block; height: auto; }
  .header__logo-text { 
    font-family: 'Outfit', sans-serif; 
    font-size: var(--f4); 
    color: var(--header-color-brown); 
    font-weight: 800; 
    line-height: 1;
  }
  
  .header__nav { 
    justify-self: center; 
    display: flex;
    align-items: center;
  }
  
  .header__menu {
    display: flex;
    align-items: center;
    gap: 40px;
    margin: 0;
    padding: 0;
    list-style: none;
  }
  
  .header__menu-item { 
    position: relative; 
    display: flex;
    align-items: center;
  }
  
  .header__menu-link,
  .header__submenu-link {
    font-family: 'Outfit', sans-serif;
    font-size: 16px;
    font-weight: 800;
    color: var(--header-color-brown);
    text-decoration: none;
    display: flex; /* Flex ajuda alinhar ao centro exatamente */
    align-items: center;
    line-height: 1; /* Tira o espaço extra da fonte em cima/embaixo */
  }
  
  .header__actions { 
    justify-self: end;
    display: flex; 
    align-items: center; 
    gap: 16px; 
  }
  
  .header__search, 
  .header__account {
    display: none;
  }
  
  .header__cart {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    background-color: transparent;
    border: 2.5px solid var(--header-color-brown);
    border-radius: 50%;
    color: var(--header-color-brown);
    text-decoration: none;
    cursor: pointer;
    box-sizing: border-box;
  }
  
  .header__cart svg { 
    width: 22px; 
    height: 22px; 
    fill: currentColor;
    display: block;
  }
  
  .header__cart-count {
    position: absolute;
    top: -6px;
    right: -6px;
    min-width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Outfit', sans-serif;
    font-size: 13px;
    font-weight: 800;
    color: #FFFFFF;
    background-color: var(--header-color-orange);
    border: 2.5px solid var(--header-color-brown);
    border-radius: 50%;
    line-height: 1;
    box-sizing: border-box;
  }

  .header__menu-toggle { 
    display: none; 
    background: none; 
    border: none; 
    cursor: pointer;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    padding: 0;
  }

  .header__burger,
  .header__burger::before,
  .header__burger::after {
    display: block;
    width: 22px;
    height: 2.5px;
    background-color: var(--header-color-brown);
    transition: transform 0.2s ease;
  }
  
  .header__burger { position: relative; }
  .header__burger::before { content: ''; position: absolute; top: -7px; }
  .header__burger::after { content: ''; position: absolute; top: 7px; }
  
  .header__mobile-nav {
    padding: 16px 20px;
    background-color: var(--header-bg);
    border-top: 1px solid var(--header-color-brown);
    border-bottom: 1px solid var(--header-color-brown);
  }
  
  .header__mobile-menu { list-style: none; margin: 0; padding: 0; }
  
  .header__mobile-link { 
    display: block; 
    padding: 12px 0; 
    font-family: 'Outfit', sans-serif;
    font-size: 18px; 
    font-weight: 800;
    color: var(--header-color-brown); 
    text-decoration: none; 
  }
  
  .header__localization { padding: 8px 0; }

  /* ============================================================
     VERSÃO MOBILE
     ============================================================ */
  @media screen and (max-width: 1000px) {
    .header {
      border-bottom: 1px solid var(--header-color-brown); /* 1px no mobile */
    }
    
    .header__inner { 
      display: flex; 
      align-items: center; /* Mantém alinhado verticalmente no centro */
      padding: 16px 16px; /* 16px lateral no mobile */
      gap: 16px; 
    }
    
    .header__nav { display: none; }
    
    /* Logo na esquerda empurrando os outros para direita */
    .header__logo-link { 
      margin: 0;
      margin-right: auto;
      order: 1; 
    }
    
    /* Ações (Carrinho) como segundo elemento */
    .header__actions {
      order: 2;
    }

    /* Menu sanduíche ativado e posicionado como último elemento */
    .header__menu-toggle { 
      display: flex; /* Mudança para flex pra garantir alinhamento do burger */
      order: 3;
    }
    
    /* Ajuste para mobile da faixa animada */
    .announcement-bar__item { padding: 0 15px; }
    .announcement-star { margin-right: 15px; }
  }
  
  @media screen and (min-width: 1001px) {
    .header__mobile-nav { display: none !important; }
  }
</style>

<script>
  (function () {
    var section = document.querySelector('.header[data-section-id="{{ section.id }}"]');
    if (!section) return;
    var toggle = section.querySelector('.header__menu-toggle');
    var nav = document.getElementById('MobileNav-{{ section.id }}');
    if (toggle && nav) {
      toggle.addEventListener('click', function () {
        var open = toggle.getAttribute('aria-expanded') === 'true';
        toggle.setAttribute('aria-expanded', String(!open));
        nav.hidden = open;
      });
    }
  })();
</script>

{% schema %}
{
  "name": "t:sections.header.name",
  "class": "section-header",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.header.settings.header__localization.content"
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "default": true,
      "label": "t:sections.header.settings.enable_country_selector.label"
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "default": true,
      "label": "t:sections.header.settings.enable_language_selector.label"
    }
  ]
}
{% endschema %}